const axios = require("axios");
//const { Companies } = require("../DB_connection");

module.exports = async (req, res) => {

    try {
        //const data = await Company.findAll();
    } catch (error) {
        //res.status(500).send(error.message);
    }

};